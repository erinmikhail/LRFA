Это короткая строка.

А это очень длинная строка, которая должна быть разбита на несколько строк по восемьдесят символов, потому что она явно превышает установленный лимит и требует правильного форматирования текста.

Еще одна короткая строка для проверки.

И снова очень длинная строка с множеством слов, которая будет разбита на несколько строк по восемьдесят символов с равномерным распределением пробелов между словами для красивого выравнивания текста.

Просто короткая.